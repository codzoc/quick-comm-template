name: Firebase Deploy

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env and .firebaserc from config
        run: |
          # Write secret to temp file to avoid shell escaping issues
          echo '${{ secrets.VITE_FIREBASE_CONFIG }}' > config.json

          # Parse Firebase config JSON and create .env file
          cat config.json | jq -r '"VITE_FIREBASE_API_KEY=" + .apiKey' > .env
          cat config.json | jq -r '"VITE_FIREBASE_AUTH_DOMAIN=" + .authDomain' >> .env
          cat config.json | jq -r '"VITE_FIREBASE_PROJECT_ID=" + .projectId' >> .env
          cat config.json | jq -r '"VITE_FIREBASE_STORAGE_BUCKET=" + .storageBucket' >> .env
          cat config.json | jq -r '"VITE_FIREBASE_MESSAGING_SENDER_ID=" + .messagingSenderId' >> .env
          cat config.json | jq -r '"VITE_FIREBASE_APP_ID=" + .appId' >> .env
          cat config.json | jq -r '"VITE_FIREBASE_MEASUREMENT_ID=" + .measurementId' >> .env

          # Create .firebaserc from config
          cat config.json | jq '{projects: {default: .projectId}}' > .firebaserc

          # Clean up temp file
          rm config.json

      - name: Debug - Verify .firebaserc created
        run: |
          echo "=== .firebaserc contents ==="
          cat .firebaserc
          echo "=== .env file exists ==="
          ls -la .env
          echo "=== Current directory files ==="
          ls -la

      - name: Build project
        run: npm run build

      - name: Copy .firebaserc to dist
        run: cp .firebaserc dist/.firebaserc

      - name: Upload build artifacts and config
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: dist/

  deploy-preview:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download build artifacts and config
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: dist/

      - name: Copy .firebaserc from dist to root
        run: cp dist/.firebaserc .firebaserc

      - name: Debug - List files
        run: |
          echo "=== Current directory ==="
          pwd
          echo "=== Files in current directory ==="
          ls -la
          echo "=== .firebaserc contents ==="
          cat .firebaserc

      - name: Extract project ID
        id: firebase-config
        run: |
          PROJECT_ID=$(cat .firebaserc | jq -r '.projects.default')
          echo "project_id=$PROJECT_ID" >> $GITHUB_OUTPUT

      - name: Deploy Firestore Rules and Indexes (Preview)
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only firestore --project ${{ steps.firebase-config.outputs.project_id }}
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Deploy to Firebase Preview Channel
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          projectId: '${{ steps.firebase-config.outputs.project_id }}'
          expires: 7d
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels

  deploy-production:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download build artifacts and config
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: dist/

      - name: Copy .firebaserc from dist to root
        run: cp dist/.firebaserc .firebaserc

      - name: Debug - List files
        run: |
          echo "=== Current directory ==="
          pwd
          echo "=== Files in current directory ==="
          ls -la
          echo "=== .firebaserc contents ==="
          cat .firebaserc

      - name: Extract project ID
        id: firebase-config
        run: |
          PROJECT_ID=$(cat .firebaserc | jq -r '.projects.default')
          echo "project_id=$PROJECT_ID" >> $GITHUB_OUTPUT

      - name: Deploy Firestore Rules and Indexes
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only firestore --project ${{ steps.firebase-config.outputs.project_id }}
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

      - name: Deploy to Firebase Hosting (Production)
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          projectId: '${{ steps.firebase-config.outputs.project_id }}'
          channelId: live
